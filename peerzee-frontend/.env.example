# Example environment variables for Peerzee Frontend

# Development (npm run dev)
# Use absolute URLs pointing to local backend
NEXT_PUBLIC_API_URL=http://localhost:9000
NEXT_PUBLIC_SOCKET_URL=http://localhost:9000

# Production (Docker)
# Use relative paths (handled by .env.production)
# NEXT_PUBLIC_API_URL=/api
# NEXT_PUBLIC_SOCKET_URL=

# Note: 
# - .env.local is used for npm dev (not tracked in git)
# - .env.production is used for Docker builds
# - Socket URL can be empty to use window.location.origin
